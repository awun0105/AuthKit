[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: integration tests using external or persistent resources
    postgres: tests that require a PostgreSQL server
    e2e: clean-consumer end-to-end tests
